# Static Gesture Recognition

A Python project for recognizing **static hand gestures** using machine learning and computer vision.

## 📘 Overview
This project classifies static hand gestures (like numbers or signs) from images.  
It includes data, models, and scripts for preprocessing, training, and prediction.

## 📂 Folder Structure
```
.
├── data/       # Gesture dataset
├── models/     # Trained/saved models
├── src/        # Source code for preprocessing, training, and prediction
└── .gitignore
```

## ⚙️ Setup
1. Clone the repository:
   ```bash
   git clone https://github.com/MananKakkar1/Static-Gesture-Recognition.git
   cd Static-Gesture-Recognition
   ```
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Place your dataset in the `data/` folder.

## 🚀 Usage
- **Train Model:** Run your training script from `src/`.
- **Predict:** Use the saved model in `models/` to classify new gestures.
- **Extend:** Add new gestures or modify the model for better accuracy.

## 🧠 Features
- Clean, modular structure  
- Simple end-to-end gesture recognition workflow  
- Easily extendable for new gestures or architectures  

## 👨‍💻 Author
Created by [Manan Kakkar](https://github.com/MananKakkar1)
